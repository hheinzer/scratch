![scratch](.logo.svg)

This repository contains small utility modules for use in larger projects. Each module is
self-contained with minimal dependencies and includes a readme file detailing how to use it. If you
see a module without one, that means it is a work in progress. I try to keep things as simple as
possible by sticking to C99, avoiding macro use, and using assert-style error handling.
